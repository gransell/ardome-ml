"""AML Mixed Environment

This module is designed to provide supplementary AML functionality - 
components defined within it will *probably* require aml and amlx 
functionality, so unlike amlx, these aren't supposed to run without a
local binary runtime of the underlying AML components."""

import amly.tkplay as tkplay
