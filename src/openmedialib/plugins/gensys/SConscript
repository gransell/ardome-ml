# (C) Ardendo SE 2008
#
# Released under the LGPL

Import(['local_env'])

local_env.packages( 'boost_thread' )

object = local_env.plugin( 'openmedialib_gensys', [ 'gensys_plugin.cpp' ] )

local_env.copy_project_files( local_env[ 'stage_ml_plugin' ], object + [ 'gensys_plugin.opl' ] )

Return( 'object' )
